#include<stdio.h>
#include<string.h>
int main()
{
	char s[100];
	int n,size,i;
	scanf("%s%d",s,&n);
	size=strlen(s);
	for(i=n;i<size;i++)
	{
		printf("%c",s[i]);
	}
	for(i=0;i<n;i++)
	{
		printf("%c",s[i]);
	}
	return 0;
}
